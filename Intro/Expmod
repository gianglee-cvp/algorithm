#include <bits/stdc++.h>
using namespace std ;

int main()
{
long long a,b ; 
cin >> a >> b ; 
int d = 1e9 +7 ;
int cmd = 1  ; 
a = a%d ;
while(b > 0){
  if(b&1){
    cmd = (cmd * a ) % d ; 
  }
  a = (a*a) % d ; 
  b = b /2 ; 
}
cout << cmd ;
}